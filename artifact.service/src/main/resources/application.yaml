spring:
  application:
    name: artifact.service

  datasource:
    url: jdbc:postgresql://localhost:5432/db_artifact
    username: aigerim
    password: password123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true


  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: cursed-group
      auto-offset-reset: earliest

jwt:
  secret: MyVerySecretKeyForJWTSigningThatIsLongEnough32chars!

server:
  port: 8081

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html