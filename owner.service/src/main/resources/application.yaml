spring:
  application:
    name: owner.service
  datasource:
    url: jdbc:postgresql://localhost:5432/db_owner
    username: aigerim
    password: password123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8082

feign:
  client:
    artifact-service:
      url: http://localhost:8081